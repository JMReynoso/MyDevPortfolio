services:
 frontend-prod:
   build:
     context: .
     dockerfile: Dockerfile.prod
   container_name: frontend-prod-environment
   restart: always
   ports:
     - 5173:5173
   networks:
     - network
networks:
 network: