services:
 frontend-prod:
   build:
     context: .
     dockerfile: Dockerfile.prod
     args:
     - VITE_WEB3FORMS_ACCESS_KEY=${VITE_WEB3FORMS_ACCESS_KEY}
   container_name: frontend-prod-environment
   restart: always
   ports:
     - 5173:5173
   networks:
     - network
networks:
 network:
